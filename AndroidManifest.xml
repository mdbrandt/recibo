<?xml version="1.0" encoding="utf-8"?>
<manifest 
	xmlns:android="http://schemas.android.com/apk/res/android"
    package="recibo.platform"
    android:versionCode="1"
    android:versionName="1.0">

	<!-- Billshare application -->
    <!-- <application 
    	android:icon="@drawable/icon" 
    	android:label="@string/app_name">
        
        <activity 
        	android:label="@string/app_name" 
        	android:name="recibo.app.billshare.Billshare">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

		<activity android:name="recibo.app.billshare.addUser">
		</activity>

        <provider 
        	android:name="recibo.platform.ReciboContentProvider"
        	android:authorities="recibo.platform.recibocontentprovider">
		</provider>
		
    </application> -->
    
    
    <!-- Origins application -->
    <application 
    	android:icon="@drawable/icon" 
    	android:label="@string/app_name">
    	
    	<activity 
    		android:label="@string/app_name" 
    		android:name="recibo.app.origins.Origins">
    		<intent-filter>
                <action android:name="android.intent.action.MAIN"  />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
    	</activity>
    	
    	<uses-library android:name="com.google.android.maps" />
    	
    </application>
    
    <!-- because Google Maps API needs network access -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    
</manifest>